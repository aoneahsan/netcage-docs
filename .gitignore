# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Yarn's local resolution cache. Machine state, not source.
.yarn/install-state.gz
.yarn/cache

# 🔴 This repository is PUBLIC. No environment file, key or credential is ever tracked here.
.env
.env.*
# ...except the example, which carries NAMES and empty values only and is the point of having one.
!.env.example
*.pem
*.p8
*.jks
*.keystore
